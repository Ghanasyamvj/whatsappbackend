############# Deployment & App configuration (examples) #############
# URL where the frontend is hosted (used for links or redirects)
FRONTEND_URL=https://whatsappflowbuilder-production.up.railway.app

# Webhook verification token (choose a secret value and set it here)
WEBHOOK_VERIFY_TOKEN=__YOUR_WEBHOOK_VERIFY_TOKEN__

# WhatsApp Business API credentials (do NOT commit these)
# Replace the placeholders with values from your WhatsApp/Railway settings
WHATSAPP_ACCESS_TOKEN=__YOUR_WHATSAPP_ACCESS_TOKEN__
WHATSAPP_API_VERSION=v22.0
WHATSAPP_APP_SECRET=__YOUR_WHATSAPP_APP_SECRET__
WHATSAPP_BUSINESS_ACCOUNT_ID=164297206767745
WHATSAPP_BUSINESS_NUMBER=15550617327
WHATSAPP_PHONE_NUMBER_ID=158282837372377

######################################################################
## Notes:
## - Never paste real secrets into Git. Use Railway (or your host) environment variables.
## - For Firebase Admin credentials prefer setting FIREBASE_SERVICE_ACCOUNT (full JSON) or
##   FIREBASE_SERVICE_ACCOUNT_BASE64 (base64-encoded JSON) in the host's secret editor.
## - If you must set FIREBASE_PRIVATE_KEY directly, ensure newlines are preserved or use base64.
######################################################################

############ Optional: individual service-account fields (match JSON keys) ############
# If you prefer to set service-account values as separate env vars, paste the matching values below.
# This mirrors the JSON format you downloaded from Firebase console. Prefer FIREBASE_SERVICE_ACCOUNT when possible.

# Example mapping (fill values from your downloaded JSON):
FIREBASE_SA_type=service_account
FIREBASE_SA_project_id=__your_project_id__
FIREBASE_SA_private_key_id=__your_private_key_id__
# For the private key include proper newlines. If your host strips newlines, escape them (\n) as shown.
FIREBASE_SA_private_key="-----BEGIN PRIVATE KEY-----\n<PASTE_PRIVATE_KEY_CONTENT_WITH_ESCAPED_NEWLINES>\n-----END PRIVATE KEY-----\n"
FIREBASE_SA_client_email=__your_client_email__
FIREBASE_SA_client_id=__your_client_id__
FIREBASE_SA_auth_uri=https://accounts.google.com/o/oauth2/auth
FIREBASE_SA_token_uri=https://oauth2.googleapis.com/token
FIREBASE_SA_auth_provider_x509_cert_url=https://www.googleapis.com/oauth2/v1/certs
FIREBASE_SA_client_x509_cert_url=__your_client_x509_cert_url__


## If you set the above individual fields, you can build the JSON in your app or paste them for debugging.
########################################################################################